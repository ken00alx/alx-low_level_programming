#include <stdio.h>

/**
 * main - lowercase alphabet reverse mode
 *
 * Return: always 0
 */

int main(void)
{
	char letter;

	for (lettr = 'z'; letter <= 'a'' letter++)
		putchar("%s", letter);

	putchar('\n');

	return (0);
}
