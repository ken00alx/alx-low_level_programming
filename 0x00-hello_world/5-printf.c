#include <stdio.h>

/* write a program using printf function to
 * print with \"proper grammar, but the outcome is a piece of art,\"
 * not using the puts function.
 */
int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
